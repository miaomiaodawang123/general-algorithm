1.给定一个正整数M, 请构造出一个长度为M的数组，要求对任意的i,j,k三个位置，如果i<j<k, 都有arr[i] + arr[k] != 2*arr[j],返回构造的arr

2.给定一个二叉树的头节点head, 路径的规定有以下三种不同的规定：
1).路径必须是头节点出发，到叶节点为止，返回最大路径和
2).路径可以从任何节点出发，但必须往下走到任何节点，返回最大路径和
3).路径可以从任何节点出发，到任何节点，返回最大路径和


3.在行列都有序的二维数组中找到num,返回true没有false

4.有n个打包机器从左到右一字排开，上方有一个自动装置会抓取一批物品放到每个打包机上，放到每个机器上的这些物品数量有多有少，
物品数量不同需要工人移动物品，物品数量相等才能打包。每次只能办一个物品移动，为了省力，只能在相邻的机器拌匀，
请计算在搬动最小轮数的前提下，使每个机器上物品数量相等，如果不能返回-1 (力扣 洗衣机问题)

5.给定一个数组arr长度为n,你可以把任意长度大于0且小于N的前缀作为左部分，剩下的作为右部分。
但是每种划分下都有左部分的最大值和右部分的最大值，请返回最大的，左部分最大值减去右部分最大值的绝对值
